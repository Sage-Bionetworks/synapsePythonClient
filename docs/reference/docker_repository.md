# DockerRepository

!!! info "v5.0.0 uses the object-oriented models"
    This page documents the legacy `DockerRepository` entity class. An object-oriented
    `DockerRepository` model is available in `synapseclient.models`.

::: synapseclient.entity.DockerRepository
