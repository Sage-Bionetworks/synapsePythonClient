# Folder

!!! info "v5.0.0 uses the object-oriented models"
    This page documents the legacy `Folder` entity class. New code should use the
    object-oriented [Folder][folder-reference-sync] model.

::: synapseclient.entity.Folder
