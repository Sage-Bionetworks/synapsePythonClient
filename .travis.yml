sudo: true
language: python

matrix:
  include:
    - os: linux
      python: 2.7.14
    - os: linux
      python: 3.5.3
    - os: linux
      python: 3.6.2
    - os: osx
      language: generic
      env: PY_VERSION=2.7.14
      python: 2.7.14

    - os: osx
      language: generic
      env: PY_VERSION=3.5.3
      python: 3.5.3

    - os: osx
      language: generic
      env: PY_VERSION=3.6.2
      python: 3.6.2


addons:
  ssh_known_hosts: ec2-54-212-85-156.us-west-2.compute.amazonaws.com

before_install: |
  if [ "$TRAVIS_OS_NAME" == "osx" ]; then
    brew update
    brew outdated pyenv || brew upgrade pyenv
    brew install pyenv-virtualenv
    pyenv install $PY_VERSION
    export PYENV_VERSION=$PY_VERSION
    export PATH="/Users/travis/.pyenv/shims:${PATH}"
    pyenv-virtualenv venv
    source venv/bin/activate
  fi

  pip install nose mock

  if [ "$TRAVIS_OS_NAME" == "linux" ]; then
    sudo apt-get update
    sudo apt-get install dbus gnome-keyring python-dbus python-gnomekeyring
  fi



  travis_wait pip install pysftp cython pandas boto3

install:
- python setup.py install

before_script:
- |
  if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then #create a default keyring
      dbus-launch /usr/bin/python -c "import gnomekeyring;gnomekeyring.create_sync('login', '');"
  fi
- |
  if [[ "${TRAVIS_PULL_REQUEST}" = "false" ]]; then
    openssl aes-256-cbc -K $encrypted_d17283647768_key -iv $encrypted_d17283647768_iv -in test.synapseConfig.enc -out test.synapseConfig -d
    mv test.synapseConfig ~/.synapseConfig
    openssl aes-256-cbc -K $encrypted_d17283647768_key -iv $encrypted_d17283647768_iv -in test.awscredentials.enc -out test.awscredentials -d
    mkdir -p ~/.aws && mv test.awscredentials ~/.aws/credentials
  fi


script:
#- nosetests -vs tests/unit
- |
  if [ "${TRAVIS_PULL_REQUEST}" = "false" ];then
    PREFIX=""
    if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then
      PREFIX="dbus-launch"
    fi

    dbus-launch python -c "import keyring; print(keyring.get_keyring()); print(keyring.get_password("asdf","asdf") is None)"
    #TODO change back
    #$PREFIX nosetests -vs tests/integration;


  else
    echo "Skipping integration tests";
  fi



